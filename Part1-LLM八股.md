# Part 1 - LLM 八股

> 本章涵盖 Transformer 架构、位置编码、注意力机制、训练与推理相关的核心知识点。

---

## 1.1 自注意力机制的工作原理及与 RNN 的比较

**参考答案：**

自注意力（Self-Attention）机制是Transformer模型的核心，它使得模型能够动态地衡量输入序列中不同单词之间的重要性，并据此生成每个单词的上下文感知表示。

**工作原理如下：**

1. **生成Q, K, V向量：** 对于输入序列中的每一个词元（token）的嵌入向量，通过乘以三个可学习的权重矩阵 $W^Q, W^K, W^V$，分别生成三个向量：查询向量（Query, Q）、键向量（Key, K）和值向量（Value, V）。
   - **Query (Q):** 代表当前词元为了更好地理解自己，需要去"查询"序列中其他词元的信息。
   - **Key (K):** 代表序列中每个词元所"携带"的，可以被查询的信息标签。
   - **Value (V):** 代表序列中每个词元实际包含的深层含义。

2. **计算注意力分数：** 计算当前词元的Q与其他所有词元的K的点积，衡量两者之间的相关性。

$$\text{Score}(Q_i, K_j) = Q_i \cdot K_j$$

3. **缩放（Scaling）：** 将分数除以缩放因子 $\sqrt{d_k}$，防止点积结果过大导致Softmax进入饱和区。

$$\frac{Q \cdot K^T}{\sqrt{d_k}}$$

4. **Softmax归一化：** 将缩放后的分数转换为注意力权重（概率分布）。

$$\text{AttentionWeights} = \text{softmax}\left(\frac{Q K^T}{\sqrt{d_k}}\right)$$

5. **加权求和：** 将注意力权重与V向量相乘并求和，得到最终输出。

$$\text{Output} = \text{AttentionWeights} \cdot V$$

**为什么比RNN更适合处理长序列？**

1. **并行计算能力：** 自注意力可以一次性处理整个序列，是高度并行的。RNN必须按时间顺序依次处理，无法并行化。
2. **解决长距离依赖问题：** 自注意力中任意两个位置的交互路径长度为O(1)，而RNN的信息传递路径为O(N)，极易导致梯度消失或梯度爆炸。

---

## 1.2 位置编码

**参考答案：**

**什么是位置编码？**

位置编码（Positional Encoding, PE）是一个与词嵌入维度相同的向量，目的是向模型注入关于词元在输入序列中绝对或相对位置的信息，它会与词元的词嵌入相加，然后一同输入到Transformer的底层。

**为什么它是必需的？**

Transformer的自注意力本身不包含任何关于词元顺序的信息，是**置换不变（Permutation-invariant）**的。语序在自然语言中至关重要（例如"我打你"和"你打我"含义完全相反），因此必须通过外部机制将位置信息显式地提供给模型。

**两种实现方式：**

**1. 正弦/余弦位置编码（Sinusoidal Positional Encoding）**

原始Transformer论文中使用的方法：

$$PE_{(pos, 2i)} = \sin(pos / 10000^{2i/d_{\text{model}}})$$

$$PE_{(pos, 2i+1)} = \cos(pos / 10000^{2i/d_{\text{model}}})$$

- **优点：** 具有可外推性（可以处理比训练时更长的序列）；$PE_{pos+k}$ 可以表示为 $PE_{pos}$ 的线性函数，便于捕捉相对位置依赖。

**2. 可学习的绝对位置编码（Learned Absolute Positional Encoding）**

将位置编码视为模型参数，通过训练学习得到。形状为 `(max_sequence_length, embedding_dimension)` 的矩阵。BERT、GPT-2采用此方式。

- **优点：** 更灵活，模型自己学习最适合数据的位置表示。
- **缺点：** 无法泛化到超过预设 `max_sequence_length` 的长度。

---

## 1.3 RoPE（旋转位置编码）

**参考答案：**

RoPE（Rotary Position Embedding）是目前大语言模型（如Llama系列、Qwen等）最主流的位置编码方案之一。

**核心思想：** 通过向量旋转的方式，将绝对位置信息编码到Query和Key向量中，从而使得模型在计算注意力分数时，能够自然地利用相对位置信息。

**工作原理：**

1. **维度分组：** 将Q和K向量的 $d$ 维特征两两一组，视为 $d/2$ 个二维向量。
2. **构造旋转矩阵：** 对于序列中的位置 $m$，构造与位置相关的旋转矩阵 $R_m$。
3. **旋转Q和K：** 将每个二维向量通过对应的旋转矩阵 $R_m$ 进行旋转。

数学上等价于将每个二维向量看作复数，然后乘以 $e^{im\theta}$。

**关键特性：** 经过旋转后，位置 $m$ 的 $q_m$ 和位置 $n$ 的 $k_n$ 进行点积运算时，其结果只与相对位置 $(m-n)$ 有关，与绝对位置无关。

**对比绝对位置编码的优劣势：**

| 维度 | RoPE |
|------|------|
| ✅ 内置相对位置建模 | 注意力分数直接依赖于词元间相对距离 |
| ✅ 良好的外推能力 | 处理比训练时更长的序列表现出色 |
| ✅ 不引入额外可训练参数 | 函数式固定编码方式 |
| ✅ 依赖性随距离衰减 | 更符合语言中远距离相关性更弱的直觉 |
| ❌ 理论理解较复杂 | 涉及复数、欧拉公式、旋转矩阵 |
| ❌ 绝对位置表征可能较弱 | 对强依赖绝对位置信息的任务效果不如绝对位置编码直观 |

---

## 1.4 MHA、MQA、GQA 的区别

**参考答案：**

三者的核心区别在于如何组织和共享Query、Key和Value的"头"（Head），目标是在模型效果和推理效率之间做出不同的权衡。

**1. MHA（Multi-Head Attention）**

- **结构：** $N$ 个Q头，$N$ 个K头，$N$ 个V头
- **优点：** 效果最好，每个头可以在不同子空间中学习到不同信息
- **缺点：** KV Cache大小与头数 $N$ 成正比，显存占用很大

**2. MQA（Multi-Query Attention）**

- **结构：** $N$ 个Q头，**1个**K头，**1个**V头（所有Q头共享同一组K/V）
- **优点：** KV Cache减小 $N$ 倍，显著降低显存占用和推理成本
- **缺点：** 所有Q头被迫从同样的K/V中提取信息，模型表达能力受限

**3. GQA（Grouped-Query Attention）**

- **结构：** $N$ 个Q头，**G个**K头，**G个**V头（每组Q头共享一组K/V）
- **说明：**
  - 当 $G=N$ 时，GQA等价于MHA
  - 当 $G=1$ 时，GQA等价于MQA
- **优点：** 推理效率远超MHA，性能优于MQA，是灵活的折中方案

| 特性 | MHA | MQA | GQA |
|------|-----|-----|-----|
| 结构 | N+N+N | N+1+1 | N+G+G |
| 模型质量 | 最高 | 可能下降 | 接近MHA |
| 推理效率 | 最低 | 最高 | 居中 |
| 应用 | BERT, GPT-3 | PaLM | Llama 2, Mixtral |

---

## 1.5 Encoder-Only、Decoder-Only、Encoder-Decoder 架构比较

**参考答案：**

**1. Encoder-Only（例如 BERT, RoBERTa）**

- **核心机制：** 双向自注意力
- **最擅长任务：** 自然语言理解（NLU）——分类、序列标注、NER、NLI、完形填空
- **原因：** 这些任务的核心是**理解**输入文本，双向上下文对于准确理解至关重要

**2. Decoder-Only（例如 GPT系列, Llama, Qwen）**

- **核心机制：** 单向（因果）自注意力，预测第 `t` 个词元时只能关注位置 `1` 到 `t-1`
- **最擅长任务：** 自然语言生成（NLG）——开放式文本生成、对话系统、代码生成、In-context Learning
- **原因：** 语言生成是顺序的、从左到右的，单向注意力完美模拟了这一过程

**3. Encoder-Decoder（例如 T5, BART, 原始Transformer）**

- **核心机制：** Encoder使用双向注意力编码输入；Decoder使用单向注意力处理已生成序列，并通过**交叉注意力（Cross-Attention）**关注Encoder的输出
- **最擅长任务：** 序列到序列（Seq2Seq）——机器翻译、文本摘要、问答
- **原因：** 这类任务需要首先对源序列有完整的全局理解（Encoder），然后基于这个理解有条件地生成目标序列（Decoder）

---

## 1.6 Scaling Laws

**参考答案：**

**什么是Scaling Laws？**

Scaling Laws（尺度定律）是由OpenAI、DeepMind等机构发现的经验性规律，揭示了模型性能（交叉熵Loss）与三个关键资源要素——**模型参数规模（N）**、**训练数据集大小（D）**、**训练所用计算量（C）**——之间存在可预测的**幂律关系（Power-Law Relationship）**。

**揭示了什么关系？**

1. **性能的可预测性：** 模型损失随N、D、C增加而平滑可预测地下降，可通过小规模实验外推大模型性能。
2. **瓶颈效应：** 模型性能会被N、D、C中最受限的那个因素制约，必须协同扩展这三个要素。
3. **资源的最优分配（Chinchilla定律）：** DeepMind指出，**为达到计算最优，模型参数量和训练数据量应按近似1:20的比例扩展**。例如，70B参数的模型约需1.4万亿个token。

**对LLM研发的指导意义：**

1. **科学指导项目规划：** 通过小规模实验拟合Scaling Law，预测最终模型性能，合理申请计算资源。
2. **优化资源配置：** Chinchilla定律告诉我们，与其训练数据不足的超大模型，不如用同样算力训练数据更充分的稍小模型。
3. **强调数据的重要性：** 高质量、大规模训练数据和模型参数规模同等重要，推动了数据工程领域的发展。

---

## 1.7 常见解码策略

**参考答案：**

**1. Greedy Search（贪心搜索）**

- **原理：** 每步选择概率最高的词元
- **优点：** 速度快，计算开销最小
- **缺点：** 局部最优，缺乏多样性

**2. Beam Search（集束搜索）**

- **原理：** 每步保留 $k$ 个最有可能的候选序列
- **优点：** 质量更高，能找到概率更高的序列
- **缺点：** 计算成本是贪心搜索的 $k$ 倍；倾向于生成安全、常见的句子；长文本易重复

**3. Top-K Sampling（Top-K 采样）**

- **原理：** 只在概率最高的K个词元中随机采样
- **优点：** 引入多样性，过滤低概率词
- **缺点：** K值固定，概率分布尖锐时K过大会引入噪声，分布平坦时K过小会限制选择

**4. Nucleus Sampling / Top-P Sampling（核心采样）**

- **原理：** 累加概率直到超过阈值 $p$，在这个动态候选集中随机采样
- **优点：** 候选集大小自适应变化；当模型确定时候选集小（生成更精确），不确定时候选集大（允许更多探索）；是大多数LLM应用默认的采样策略

---

## 1.8 词元化：BPE 与 WordPiece

**参考答案：**

**什么是词元化？** 将原始文本字符串分解成独立的词元（token）并映射到整数ID的过程，是模型处理文本的第一步。

**现代LLM采用子词（Subword）算法的好处：**
- 有效处理未登录词（OOV）
- 平衡词表大小与序列长度
- 保留形态信息（"running"、"runner"共享"run"这一子词）

**BPE vs. WordPiece 主要区别：**

| 特性 | BPE | WordPiece |
|------|-----|-----------|
| 合并决策标准 | **频率驱动**：合并出现次数最多的相邻子词对 | **似然驱动**：合并能最大化提升语料库语言模型似然的子词对 |
| 理论基础 | 数据压缩算法，简单高效 | 概率语言模型，理论上更优 |
| 应用代表 | GPT, Llama, RoBERTa | BERT, T5 |

---

## 1.9 NLP 与 LLM 的区别

**参考答案：**

LLM是NLP发展至今最前沿的技术范式，它在很大程度上重塑了NLP领域。两者是领域与技术、一般与具体的关系。

**共同之处：** 最终目标一致（实现AI对人类语言的理解与生成）；都建立在深度学习和Transformer架构的基础上。

**最大区别：研究和应用范式的根本性转变**，从"为每个任务训练一个模型"转向"用一个模型解决所有任务"。

| 维度 | 传统NLP | LLM |
|------|---------|-----|
| 任务处理 | 分而治之，每个任务单独训练"专家"模型 | 通用模型，通过Prompt/In-context Learning引导 |
| 模型能力 | 能力明确且有限，与训练目标直接相关 | 规模达到阈值后出现**"涌现能力"** |
| 规模 | 百万到几亿参数 | 百亿到万亿参数 |
| 交互方式 | API集成，输入输出格式固定 | 以**对话和指令**为核心的全新交互方式 |

---

## 1.10 L1 和 L2 正则化

**参考答案：**

两者均通过在损失函数中添加惩罚项来防止模型过拟合。

**L1 正则化（Lasso）**

$$\text{Loss}_{L1} = \text{Original Loss} + \lambda \sum_{i} |w_i|$$

- **核心作用：** 产生稀疏性（Sparsity），将不重要特征的权重精确变为0
- **适用场景：** 特征选择，数据中含大量冗余特征时

**L2 正则化（Ridge / Weight Decay）**

$$\text{Loss}_{L2} = \text{Original Loss} + \lambda \sum_{i} w_i^2$$

- **核心作用：** 权重衰减（Weight Decay），促使权重趋近于0但通常不等于0
- **适用场景：** 通用过拟合防治，深度学习中的"权重衰减"通常即指L2

| 对比项 | L1 | L2 |
|--------|----|----|
| 惩罚项 | 权重绝对值之和 | 权重平方和 |
| 效果 | 权重稀疏化，部分为0 | 权重平滑化，趋近于0 |
| 主要用途 | 特征选择 | 防止过拟合，提升泛化 |
| 解的特性 | 不稳定 | 稳定，解是唯一的 |

---

## 1.11 涌现能力

**参考答案：**

**对"涌现能力"的理解：** 在小型模型中不存在或表现不佳，但当模型规模（参数量、训练数据和计算量）达到某个临界点后，突然出现并显著超越随机水平的能力。其核心特征是**非线性和不可预测性**（"相变"式的跃迁）。

**典型涌现能力例子：**
- **思维链（Chain-of-Thought, CoT）：** 小模型无法利用"一步一步思考"的提示，大模型可以
- **上下文学习（In-context Learning）：** 无需更新权重，通过Few-shot示例学会新任务
- **执行复杂指令：** 理解并遵循包含多个步骤、约束和否定逻辑的指令

**出现的模型规模：** 没有固定数值，取决于能力本身和评估任务。根据Google PaLM的研究，思维链推理通常在模型参数规模达到**百亿到千亿（62B+）**级别时开始出现，在8B和16B的模型上则完全无效。

---

## 1.12 LLM 常用激活函数

**参考答案：**

激活函数的主要作用是为网络引入**非线性（non-linearity）**，否则多层神经网络本质上等同于单层线性模型。

**1. GeLU（Gaussian Error Linear Unit）**

- 公式：$x \cdot \Phi(x)$，其中 $\Phi(x)$ 是高斯分布的累积分布函数
- 应用：BERT、GPT-2
- **为什么选用：** 是ReLU的平滑近似，梯度更稳定，更有利于模型收敛；综合了Dropout和ReLU的思想

**2. SwiGLU（Swish-Gated Linear Unit）**

- 公式：$Swish(A) \otimes B$，其中 $Swish(x) = x \cdot \sigma(x)$
- 应用：Llama、PaLM、Mixtral、Gemma（当前最主流）
- **为什么选用：**
  - **门控机制：** B部分作为动态的"门"，根据输入内容控制信息的通过，显著增强FFN层的表达能力
  - **实证效果优越：** Google PaLM论文中的实验证明，使用SwiGLU可显著降低模型困惑度

---

## 1.13 混合专家模型（MoE）

**参考答案：**

MoE（Mixture of Experts）的核心思想是通过**稀疏激活（Sparse Activation）**，允许模型拥有巨大的总参数量，但在处理任何一个输入时只动用其中一小部分参数。

**工作原理：**

1. **用"专家"替换FFN层：** 将部分/全部FFN层替换为MoE层，每个MoE层由N个独立的FFN"专家"和1个"路由器（Router）"组成
2. **动态路由决策：** Token经过路由器，输出N个分数代表与各专家的"匹配度"
3. **Top-K稀疏激活：** 只激活分数最高的Top-K个专家（通常K=1或2），其余专家不参与计算
4. **加权输出：** K个专家输出的加权和作为最终结果

**如何实现"参数大但成本低"？**

以Mixtral-8x7B为例（8个专家，每次激活2个）：
- **总参数量：** 包含所有8个专家（约47B）
- **推理成本：** 每次只激活2个专家，计算量约等于一个13B的稠密模型

MoE成功地将**总参数量**（知识容量）和**单次推理计算量**（推理成本）**解耦**，实现"用小模型的成本，获得大模型的知识"。

---

## 1.14 训练百/千亿参数LLM的主要挑战

**参考答案：**

**1. 显存挑战（Memory Wall）**

- **问题：** 千亿参数模型的参数、梯度、优化器状态需要数TB存储，远超单张GPU显存
- **解决方案（3D并行）：**
  - **数据并行（DP）：** 每张卡保留完整模型副本，分割数据批次，通过All-Reduce同步梯度（不能解决单卡显存不足）
  - **流水线并行（PP）：** 将模型层垂直切分到不同GPU，可有效降低单卡显存，但会引入"流水线气泡"
  - **张量并行（TP）：** 将单个大算子水平切分，可降低单卡显存，但引入高通信开销
  - **ZeRO：** 将优化器状态、梯度、模型参数切分分布到所有GPU，极大降低单卡冗余

**2. 通信挑战（Communication Bottleneck）**

- **问题：** 大规模并行训练中通信时间可能超过计算时间
- **解决方案：** 硬件层面使用NVLink/InfiniBand；软件层面将**计算和通信操作重叠（overlap）**以隐藏通信延迟

**3. 训练不稳定性（Training Instability）**

- **问题：** 容易出现梯度爆炸/消失，Loss突然飙升为NaN
- **解决方案：**
  - **BF16混合精度训练：** 比FP16有更大动态范围，避免梯度下溢
  - **Pre-LayerNorm：** 在自注意力和FFN之前进行层归一化，结构更稳定
  - **梯度裁剪（Gradient Clipping）：** 防止梯度爆炸最直接有效的方法
  - **学习率调度与预热（Warmup）：** 训练初期使用小学习率并逐渐增大

---

## 1.15 开源框架了解

**参考答案：**

- **基础框架：** PyTorch（事实标准）
- **模型与生态：** Hugging Face Transformers（最重要的模型库）
- **大规模训练：** DeepSpeed（微软）、Megatron-LM（英伟达）——实现3D并行、ZeRO等关键技术
- **高效推理：** vLLM、TensorRT-LLM——通过PagedAttention等技术解决KV Cache显存瓶颈

**Qwen系列** / **Deepseek系列** — 可参考开源论文自行补充（Qwen2.5、Qwen3、GRPO等）

---

## 1.16 前沿论文分享（开放题）

> **提示：** 选择1-2篇自己真正理解的、有影响力的近期论文，结构化地阐述：针对什么问题、提出什么方法、对比实验有哪些、核心结论是什么。
