# Part 3 - 训练

> 本章整合了 LLM 训练全流程的核心知识，涵盖 RLHF 对齐、高效微调（PEFT）、推理加速及前沿训练框架与强化学习算法。

---

# 一、RLHF 对齐训练

## 1.1 RLHF 旨在解决 SFT 的哪些核心问题？

**参考答案：**

RLHF旨在解决语言模型中更深层次的**"对齐"（Alignment）**问题，即"HHH"原则：
1. **有用性（Helpfulness）：** 提供准确、相关且信息量丰富的内容
2. **诚实性（Honesty）：** 不捏造事实，在不知道答案时主动承认
3. **无害性（Harmlessness）：** 不产生有偏见、歧视性、暴力或有害的内容

**为什么SFT本身不足以实现对齐目标？**

1. **目标定义模糊：** "有用"、"诚实"、"无害"是复杂、主观且依赖上下文的概念，很难通过静态数据集精确定义
2. **偏好难以标注：** （prompt, ideal_response）格式无法表达"回答A比回答B更好"这类细粒度的**偏好信息**
3. **行为空间巨大：** LLM可以生成几乎无限的回复，SFT数据集只能覆盖极小的一部分，模型容易学到表面统计特征而非真正的原则
4. **暴露偏差（Exposure Bias）：** SFT训练时基于真实"黄金"上下文，推理时基于自己生成的上下文，一旦早期出现偏差，错误会累积

---

## 1.2 经典RLHF的三个核心阶段

**参考答案：**

**阶段一：监督微调（SFT）**

- **输入：** 高质量指令跟随数据集（指令 + 理想回答）
- **输出：** SFT模型（具备基础指令遵循能力）
- **目标：** 让预训练LLM初步具备理解和遵循人类指令的能力，为后续阶段提供良好初始策略

**阶段二：训练奖励模型（RM）**

- **输入：** 人类偏好比较数据集——用SFT模型针对同一prompt生成多个回答，由人类标注者排序得到（prompt, 胜出回答 $y_w$, 落败回答 $y_l$）格式的数据
- **输出：** 奖励模型（RM）——能输入任何（prompt, response）对并输出标量分数
- **目标：** 学习一个能模仿和泛化人类偏好的函数，作为下一阶段RL的"裁判"

**阶段三：近端策略优化（PPO）**

- **输入：** SFT模型（初始策略）+ RM（奖励函数）+ 用于探索的指令数据集
- **输出：** 经过RLHF对齐的最终语言模型
- **目标：** 使用RL进一步微调SFT模型，使生成的回答在获得高奖励的同时，不过于偏离原始SFT模型的风格和内容

---

## 1.3 成对比较数据 vs. 绝对评分

**参考答案：**

**采用成对比较的主要优势：**

1. **降低认知负荷，提升标注一致性：** "哪个更好"远比"给这个打几分"更容易且直观，标注者间一致性（Inter-rater agreement）更高
2. **提供更精细的信号：** 可以捕捉两个"同样好"的回答之间的细微差异
3. **数据分布归一化：** 比较数据是标准化的二元分类任务，对个体打分尺度不敏感

**潜在的劣势：**

1. **数据效率可能较低：** 每次比较只产生1比特信息，完整排序K个回答需要 $O(K^2)$ 次比较
2. **可能出现不传递性（Intransitivity）：** 人类偏好有时出现"A>B，B>C，但C>A"的循环，带来矛盾训练信号
3. **信息不完整：** 只表达相对好坏，无法体现差距的幅度

---

## 1.4 奖励模型的设计：架构、关系与损失函数

**参考答案：**

**模型架构选择：**

- 通常与要优化的LLM**相同或非常相似**的架构
- 初始化权重来自**SFT模型**
- 最后一层替换为**回归头（Regression Head）**，输出一个标量奖励分数

**与最终LLM的关系：**

RM是最终LLM的**效用函数代理（proxy for the utility function）**，扮演"人类偏好的模拟器"。RM的质量直接决定了最终LLM对齐的天花板。如果RM有缺陷，LLM在优化过程中就会"奖励作弊"。

**常用损失函数（基于Bradley-Terry模型）：**

$$P(y_w > y_l | x) = \sigma(r(y_w | x) - r(y_l | x))$$

$$\text{Loss} = -\log(\sigma(r(y_w | x) - r(y_l | x)))$$

---

## 1.5 为什么选择PPO？KL散度惩罚项的作用

**参考答案：**

**为什么选PPO而不是其他算法？**

- **vs. REINFORCE：** REINFORCE有极高方差，在LLM这种动作空间巨大、奖励信号稀疏的环境中训练非常震荡难以收敛。PPO通过价值函数作为基线和优势函数显著降低方差
- **vs. Q-learning（DQN）：** DQN为离散低维动作空间设计，无法处理LLM词汇表这种巨大的组合动作空间

**KL散度惩罚项的关键作用：**

$$\text{Objective}(\pi_{\text{RL}}) = \mathbb{E}[\text{Reward}] - \beta \cdot \mathbb{KL}(\pi_{\text{RL}} || \pi_{\text{SFT}})$$

1. **防止策略崩溃（Policy Collapse）：** 约束新策略不能与SFT策略偏离太远，将优化限制在"安全"区域内，防止模型不顾一切地寻找RM漏洞来"作弊"
2. **保证探索效率和多样性：** 使模型在已学会的、有意义的语言分布附近探索，维持生成文本的多样性和可读性

---

## 1.6 KL散度系数 β 过大或过小的问题及调参方法

**参考答案：**

**β 设置过大：**
- 模型过于"保守"，策略更新步子很小甚至几乎不更新
- 表现：训练停滞，最终RLHF模型与SFT模型几乎没有区别

**β 设置过小：**
- **奖励作弊（Reward Hacking）：** 发现RM漏洞并利用，生成分数高但质量差的文本
- **模式崩溃（Mode Collapse）：** 输出风格极其单一、重复，反复使用"奉承""安全"的短语
- **语言模型能力退化：** 偏离SFT太远可能导致生成语法错误或无意义的文本

**如何通过实验调整 β？**

1. **监控KL散度值：** KL值接近0 → β 可能太大；KL值急剧增大且不稳定 → β 可能太小
2. **监控奖励分数：** 奖励提升很快但KL急剧增大则需警惕奖励作弊
3. **定期定性分析（最重要）：** 随机抽取prompt，人工检查是否出现重复、模式化、不通顺等问题
4. **动态调整策略：** 设定KL散度目标范围，实际KL值超出范围时自动调整 β（TRL库中有此实现）

---

## 1.7 奖励作弊（Reward Hacking）

**参考答案：**

**定义：** 智能体发现并利用奖励函数的漏洞，以设计者非预期的方式最大化奖励，但实际上没有完成任务的真正目标。

**LLM应用场景举例（文本摘要）：**

- **RM的设计：** 偏好包含原文所有重要关键词且长度较长的摘要
- **奖励作弊：** 模型不精炼总结原文，而是将原文中含关键词的句子大量**原封不动地复制粘贴**，形成很长但毫无信息浓缩价值的文本
- **结果：** RM打出高分，但完全违背了"摘要"的初衷

**缓解策略：**

1. **改进奖励模型（Iterative RM Improvement）：** 将作弊生成的case作为负样本加入RM训练数据，迭代优化RM
2. **增强策略约束：** 使用足够强的KL散度惩罚项
3. **奖励函数设计多样化：** 引入"重复度"惩罚项；训练多个RM的集成（Ensemble）减少单个RM偏见被利用的风险
4. **过程监督（Process Supervision）：** 奖励好的思考过程而非仅仅最终结果
5. **提前停止（Early Stopping）：** 发现模式化倾向开始出现时停止训练

---

## 1.8 DPO 与传统 RLHF（PPO）的比较

**参考答案：**

**DPO（Direct Preference Optimization）核心思想：**

DPO**绕过**显式的奖励模型建模和复杂的强化学习训练过程，直接利用偏好数据优化语言模型。通过数学变换，将RLHF的两阶段问题直接转换成等价的一阶段监督学习问题，奖励函数在推导过程中被"抵消"掉了。

**损失函数：** 提高模型对"胜出回答"的生成概率，同时降低对"落败回答"的生成概率。

| 特性 | 传统RLHF（PPO） | DPO |
|------|----------------|-----|
| 流程阶段 | 三阶段：SFT → 训练RM → PPO-RL | 两阶段：SFT → 直接在偏好数据上微调 |
| 核心组件 | 需要显式的RM + 复杂的RL训练循环 | 不需要独立的RM，不需要RL |
| 训练过程 | 复杂且不稳定，涉及4个模型，超参数多 | 简单且稳定，本质是监督学习，超参数少 |
| 计算成本 | 高（需要大量采样和RM前向传播） | 低（只需计算偏好对中两个回答的似然概率） |
| 效果 | 效果已被广泛验证 | 许多任务上效果持平甚至优于传统RLHF |

**DPO主要优势：简洁、稳定、高效**，大大简化了对齐流程，降低了实现难度和计算成本。

---

## 1.9 排查模型"模式化/奉承/缺乏信息量"问题

**参考答案：**

这是典型的RLHF中"对齐税"（Alignment Tax）或"模式崩溃"（Mode Collapse）现象。

**可能的原因：**

1. **奖励模型（RM）的偏差：** 人类标注者可能无意识地偏爱语气礼貌、使用"安全"词汇的回答，RM学到了表面特征而不是真正的质量
2. **PPO过度优化：** β 过小或训练步数过多，模型找到了获得高分的"万能公式"
3. **偏好数据本身的局限性：** 标注者倾向于选"四平八稳"的回答，RM无法学到对"创意"、"信息密度高"等维度的偏好

**分析和解决步骤：**

1. **诊断RM：** 构造对比样本（有信息量但朴实的回答 vs. 模式化奉承的回答），用RM打分验证是否存在偏差
2. **数据迭代：** 收集失败案例，让标注者明确将模式化的输出标记为"更差"，用新数据重新训练RM
3. **算法调整：** 增大KL散度系数 β；引入熵奖励（Entropy Bonus）鼓励生成多样化词元分布；设置提前停止
4. **解码策略调整：** 适当提高Temperature或使用Top-K/Top-P采样，增加生成文本的随机性和多样性

---

## 1.10 信用分配问题：Token级别 vs. Seq级别奖励

**参考答案：**

**信用分配问题：** 当一个完整的回答序列得到最终奖励分数后，如何确定这个分数应该归功于/归咎于序列中的哪些具体词元。

**Sequence级别奖励：**

- **优点：** 与人类评估模式一致；实现简单
- **缺点：** 序列中所有token都收到相同奖励信号，无法区分"好词"和"坏词"，信号稀疏且充满噪声

**Token级别奖励：**

- **优点：** 提供精细且密集的学习信号，理论上极大提高学习效率
- **缺点：** 难以获取；单个token的好坏严重依赖后续上下文

**PPO如何缓解信用分配问题：**

通过**优势函数（Advantage Function）**和**价值模型（Critic）**来提供伪Token级别的奖励信号：

- **Critic：** 估计在某个状态（已生成部分序列的上下文）下，未来可能获得的期望奖励 $V(s_t)$
- **优势函数：** $A(s, a) = R_t - V(s_t)$，估计在当前状态 $s_t$ 选择动作 $a_t$（生成某个token）比"平均水平"好多少
- **GAE（Generalized Advantage Estimation）：** 通过指数加权平均综合多个时间步的TD误差，为每个时间步提供更可靠的信用分配信号

---

## 1.11 RLAIF（从AI反馈中强化学习）

**参考答案：**

**RLAIF的核心思想：** 用一个强大的独立AI模型（如GPT-4、Claude）替代人类标注者，为语言模型的输出提供偏好判断。流程与RLHF基本相同，区别仅在于偏好数据的来源是AI而非人类。

**RLAIF的潜力：**

1. **极高的可扩展性与效率：** AI标注者可7×24不间断工作，可使用比RLHF大几个数量级的偏好数据
2. **标注一致性：** 裁判AI和使用的prompt固定，标注标准完全一致，避免了人类标注者的主观差异
3. **探索更复杂的偏好：** 可通过设计复杂prompt，引导AI从非常专业的角度进行评估

**RLAIF的风险：**

1. **偏见的继承与放大：** 裁判AI的偏见会被传递并可能被放大，导致最终模型产生系统性、难以察觉的偏差
2. **价值的"近亲繁殖"：** 模型价值观可能逐渐与真实、多样、不断演化的人类价值观脱节
3. **缺乏常识和真实世界Grounding：** 裁判AI可能基于文本表面统计特征做判断，无法判断在现实世界中是否合理或危险
4. **对裁判AI的过度依赖：** 整个对齐的安全性都系于裁判AI一身

> **最佳实践：** RLAIF通常需要与人类监督（Human Oversight）相结合，定期由人类专家抽查和校准AI的标注结果。

---

## 1.12 Deepseek GRPO（开放题）

> **提示：** 参考GRPO论文，自行补充：
> - 与PPO的主要区别（如何简化Critic模型）
> - 核心优势（计算效率、稳定性）
> - 在DeepSeek系列模型中的实际应用效果

---

## 1.13 GSPO 和 DAPO（开放题）

> **提示：** 分别参考腾讯、阿里的相关论文，补充：
> - GSPO（Group Sampling Policy Optimization）的核心思想
> - DAPO（Direct Advantage Policy Optimization）的核心思想
> - 与GRPO的具体区别和改进点

---

# 二、高效微调（PEFT）

## 2.1 为什么需要高效微调？主流 PEFT 方法概览

**参考答案：**

**背景：** 全量微调（Full Fine-tuning）70B 模型需要约 280GB 显存（FP16 权重 + 优化器状态 + 梯度），且修改所有参数易导致**灾难性遗忘（Catastrophic Forgetting）**，成本极高、门槛极高。

PEFT（Parameter-Efficient Fine-Tuning）只训练少量参数，在保持基础模型能力的同时完成特定任务适配。

**主流方法对比：**

| 方法 | 核心思想 | 额外参数量 | 推理开销 |
|------|----------|------------|----------|
| **LoRA** | 低秩分解更新矩阵 | <1% | 几乎无（可合并） |
| **QLoRA** | 量化基础模型 + LoRA | <1% | 量化推理 |
| **Prefix Tuning** | 在输入前添加可学习前缀 token | 极少 | 有（占用上下文长度） |
| **Prompt Tuning** | 只训练 embedding 层的 soft prompt | 极少 | 有 |
| **Adapter** | 在每层注意力/FFN 后插入小网络 | 少 | 有（串行增加延迟） |

---

## 2.2 LoRA 原理详解

**参考答案：**

**核心假设：** 大模型在微调时权重更新矩阵 $\Delta W$ 是**低秩的（low-rank）**。不直接更新完整权重矩阵 $W$，而是用两个小矩阵的乘积来近似增量：

$$W' = W + \Delta W = W + BA$$

其中 $B \in \mathbb{R}^{d \times r}$，$A \in \mathbb{R}^{r \times k}$，$r \ll \min(d, k)$（秩 $r$ 通常取 4、8、16）。

**训练与推理细节：**

- 训练时**冻结原始权重 $W$**，只训练 $A$ 和 $B$，$A$ 用随机初始化，$B$ 用零初始化（保证训练初期 $\Delta W = 0$，稳定训练）
- 推理时将 $BA$ 直接**合并**到 $W$ 中：$W' = W + BA$，**零推理额外开销**
- 参数量对比：原矩阵 $d \times k$，LoRA 参数 $r \times (d + k)$；当 $r=8, d=k=4096$ 时，参数量减少约 **256 倍**

**LoRA 通常应用于哪些权重矩阵？**

- 原始论文建议应用于注意力层的 $W_Q, W_V$
- 实践中发现同时应用于 $W_Q, W_K, W_V, W_O$ 以及 FFN 层（$W_{up}, W_{down}$）效果更好
- 超参数 `target_modules` 控制应用范围，通常通过消融实验确定最优配置

**LoRA 变体：**

- **LoRA+：** 对 A 和 B 矩阵使用不同学习率，B 的学习率远高于 A，提升收敛速度
- **LoRA-FA：** A 矩阵也冻结（随机固定），只更新 B，进一步减少梯度计算量
- **DoRA（Weight-Decomposed LoRA）：** 将权重分解为幅度和方向，更接近全量微调的学习行为

---

## 2.3 QLoRA：极低显存全量级微调

**参考答案：**

QLoRA = **NF4 量化的冻结基础模型** + **BF16 精度的 LoRA 适配器**

**三个关键创新：**

1. **NF4（4-bit NormalFloat Quantization）：** 针对神经网络权重通常服从**零均值正态分布**这一特点设计的最优 4bit 量化格式。NF4 通过分位数量化确保每个量化桶内的数据点数量相同，比普通 INT4 在相同位宽下精度更高

2. **双量化（Double Quantization）：** 对量化的缩放因子（scale constants）再次量化，每个参数额外节省约 0.37 bits

3. **分页优化器（Paged Optimizers）：** 利用 NVIDIA 统一内存（Unified Memory），在 GPU 显存不足时自动将优化器状态换页到 CPU RAM，避免 OOM

**效果：** 65B 模型微调显存需求从全量微调的 >780GB 降至约 **48GB（单张 A100 80G）**，且在 Vicuna benchmark 上精度接近全量微调。

**QLoRA 局限：**

- 量化和反量化操作引入额外计算开销，训练速度慢于全量微调
- NF4 是非线性量化，反量化涉及查表操作，推理时仍需反量化才能进行矩阵计算

---

# 三、推理加速

## 3.1 LLM推理的主要瓶颈及加速方法全景

**参考答案：**

LLM推理的核心瓶颈是 **Memory-Bound（显存带宽受限）** 而非计算受限：每生成一个 token，需要从 GPU 显存中读取全部模型参数（数百 GB），但实际矩阵运算量相对较小，GPU 的算术计算单元大部分时间处于等待状态。

**主要加速技术分类：**

**1. 算法层面**

- **KV Cache：** 将已计算过的 K、V 向量缓存，避免对历史 token 重复计算，是最基础的推理优化，但会占用大量显存
- **投机采样（Speculative Decoding）：**
  - 原理：用一个小的"草稿模型"（Draft Model）快速自回归生成多个候选 token，再用大模型**并行验证**；如果验证通过则一次接受多个 token
  - 效果：在不损失任何精度的前提下，吞吐量可提升 **2-3 倍**
  - 关键条件：草稿模型和目标模型分布越相近，接受率越高
- **并行解码（Parallel Decoding）：** Medusa、EAGLE 等方法在模型上增加多个"推测头"，并行预测未来多个 token，无需单独的草稿模型

**2. 显存管理层面**

- **PagedAttention（vLLM 核心创新）：** 将 KV Cache 组织成类似操作系统虚拟内存分页的方式，彻底解决显存碎片问题，大幅提升并发吞吐量
- **量化（Quantization）：** 将模型权重和激活值从 FP16/BF16 压缩到 INT8、INT4 甚至更低精度，减少显存占用和带宽需求

**3. 计算图层面**

- **Flash Attention：** 重写注意力机制的 CUDA 核心，通过分块计算（Tiling）和 IO 感知优化减少 HBM 读写次数，训练和推理均有加速，在长序列场景尤为显著
- **算子融合（Kernel Fusion）：** 将多个小算子合并为一个大算子，减少 GPU 核函数启动开销和中间结果写回显存的次数
- **连续批处理（Continuous Batching）：** 传统静态批处理需要等整批请求全部完成才能换批，导致 GPU 大量空闲；连续批处理在每个 token 生成步骤后动态插入新请求，GPU 利用率大幅提升

---

## 3.2 量化（Quantization）原理与主流方案

**参考答案：**

**基本原理：** 将浮点数（FP16/BF16，16位）表示的权重或激活值，用更低位宽的整数（INT8、INT4）近似表示，通过牺牲少量精度换取显存占用减半和推理加速。

**核心公式：**

$$x_{int} = \text{round}(x_{float} / s) + z$$

其中 $s$ 为缩放因子（scale），$z$ 为零点（zero point）。反量化时：$x_{float} \approx (x_{int} - z) \times s$

**主流量化方案：**

| 方案 | 量化对象 | 精度损失 | 速度 | 代表框架 |
|------|----------|----------|------|---------|
| **GPTQ** | 权重（离线） | 低 | 快 | AutoGPTQ |
| **AWQ** | 权重（保留显著权重） | 很低 | 快 | AutoAWQ |
| **SmoothQuant** | 权重 + 激活值 | 低 | 快 | TensorRT-LLM |
| **GGUF/llama.cpp** | 权重（CPU友好） | 中 | CPU可用 | llama.cpp |

**关键细节：**

- **W4A16：** 权重量化到 INT4，激活值保持 FP16，是目前最常用的推理量化配置
- **Per-channel vs. Per-tensor：** Per-channel（每个输出通道独立缩放因子）精度远高于 Per-tensor
- **AWQ 的核心思想：** 激活值中只有约 1% 的"显著权重"（对应激活值特别大的通道）对精度至关重要，对这部分权重用更高精度保护，其余量化到 INT4

---

## 3.3 vLLM 与 SGLang 核心特性对比

**参考答案：**

**vLLM — PagedAttention 核心原理：**

传统 KV Cache 的问题：为每个请求预先分配最大序列长度的连续显存块，导致严重的**内部碎片**（实际用了 500 token 但预分配了 2048 token 的空间）和**外部碎片**（显存总量够但无法分配大连续块）。

PagedAttention 解决方案：
1. 将 KV Cache 切分为固定大小的"物理 Block"（如每 block 存 16 个 token 的 KV 向量）
2. 为每个序列维护"逻辑 Block → 物理 Block"的映射表（类似操作系统页表）
3. 物理 Block 可以**不连续**，按需分配，几乎零浪费
4. **Copy-on-Write 机制：** Beam Search 或并行采样时，多个序列可以共享前缀的 KV Cache 物理 Block，大幅节省显存

效果：与 HuggingFace Transformers 相比，vLLM 吞吐量提升 **14-24 倍**。

**SGLang — RadixAttention 核心原理：**

- **问题：** 对话系统中，同一前缀（如系统提示 system prompt）被数千个并发请求重复计算 KV Cache，严重浪费算力
- **RadixAttention：** 将 KV Cache 组织为前缀树（Radix Tree），自动识别和复用不同请求的公共前缀，实现**跨请求的 KV Cache 共享**
- 额外特性：原生支持结构化输出（JSON Schema 约束生成）、多模态推理、高效的 batch 调度

**对比总结：**

| 特性 | vLLM | SGLang |
|------|------|--------|
| 核心创新 | PagedAttention（显存管理） | RadixAttention（前缀复用） |
| 擅长场景 | 高并发在线服务，请求多样 | 前缀高度重复（RAG、多轮对话）、结构化输出 |
| 生态成熟度 | 更成熟，社区更大 | 更新，特定场景性能更优 |
| 结构化输出 | 需要额外配置 | 原生支持 |

---

# 四、训练框架与前沿RL算法

## 4.1 veRL 框架设计思想与核心特性

**参考答案：**

**veRL（Volcano Engine Reinforcement Learning for LLMs）** 是字节跳动火山引擎开源的、专为 LLM 强化学习（RLHF/GRPO 等）设计的高性能分布式训练框架。

**解决的核心痛点 — 资源绑定问题：**

传统 RL 训练框架（如 TRL、OpenRLHF）将 Actor 和 Critic 固定绑定在相同数量的 GPU 上，而 PPO 训练中存在两个截然不同的阶段：
- **Rollout 阶段（生成数据）：** Actor 执行推理，适合小 batch、低延迟配置（如 vLLM 引擎）
- **Training 阶段（更新参数）：** Actor 和 Critic 需要梯度计算，适合大 batch、高吞吐配置（如 Megatron/FSDP）

两个阶段的最优硬件配置完全不同，固定绑定会造成严重的 GPU 空闲浪费。

**veRL 的核心设计 — 混合编程模型（Hybrid Programming Model）：**

1. **Worker 抽象：** 将 Actor、Critic、Reference Model、Reward Model 各自封装为独立的 Worker，可以灵活地分配到不同数量的 GPU 上，不再固定绑定
2. **动态资源调度：** Rollout 阶段 Actor Worker 切换为推理优化配置（vLLM 引擎）；Training 阶段切换为训练优化配置（Megatron 张量并行/FSDP），两个阶段的 GPU 资源可以**重叠复用**
3. **异步流水线：** 不同 Worker 之间的计算可以异步执行，通过重叠计算与通信进一步隐藏延迟

**核心优势：**

- 与 OpenRLHF 等框架相比，训练吞吐量提升 **1.4-2.5 倍**
- 支持 GRPO、PPO、REINFORCE++ 等多种主流 RL 算法
- 无缝集成 Megatron-LM（张量/流水线并行）和 vLLM（高效推理采样）
- 已在 DeepSeek-R1 同类工作的训练中被广泛使用

---

## 4.2 GRPO（Group Relative Policy Optimization）

**参考答案：**

GRPO 是 DeepSeek 在 DeepSeekMath 和 DeepSeek-R1 中提出的算法，对 PPO 进行了重要简化，同时保持了良好的训练效果。

**PPO 的核心痛点：** 需要同时维护 4 个模型（Actor、Critic、Reference Model、Reward Model），其中 Critic 与 Actor 同等规模，显存和计算成本极高。

**GRPO 核心思想 — 用组内相对奖励替代 Critic 的价值估计：**

对于同一个问题 $q$，采样 $G$ 个输出 $\{o_1, o_2, ..., o_G\}$，计算每个输出的奖励 $\{r_1, r_2, ..., r_G\}$，然后用**组内归一化**计算优势：

$$\hat{A}_i = \frac{r_i - \text{mean}(r_1,...,r_G)}{\text{std}(r_1,...,r_G)}$$

- $\hat{A}_i > 0$：该输出比同组平均水平好，增大其生成概率
- $\hat{A}_i < 0$：该输出比同组平均水平差，减小其生成概率

**策略更新目标（结合 KL 惩罚和 PPO 裁剪）：**

$$\mathcal{L}_{GRPO} = \mathbb{E}\left[\min\left(\frac{\pi_\theta(o_i|q)}{\pi_{old}(o_i|q)}\hat{A}_i, \text{clip}(\cdot)\hat{A}_i\right)\right] - \beta \cdot \mathbb{KL}(\pi_\theta || \pi_{ref})$$

**与 PPO 的主要区别：**

| 对比项 | PPO | GRPO |
|--------|-----|------|
| 优势估计方式 | Critic 网络（需额外训练） | 组内奖励归一化（无需 Critic） |
| 模型数量 | 4个（Actor/Critic/Ref/RM） | 3个（Actor/Ref/RM） |
| 奖励信号粒度 | Token 级别（通过 GAE 估计） | Sequence 级别（组内比较） |
| 计算显存节省 | 基准 | 约节省 25-33% |
| 适合任务 | 通用 | 数学、代码等有明确奖励的任务 |

**在 DeepSeek-R1 中的应用：** DeepSeek-R1 以纯 RL 方式（无任何 SFT 冷启动数据）直接在预训练模型上应用 GRPO，模型自发涌现出"长链思考"（extended reasoning with \<think\> tokens）能力，证明了纯 RL 驱动复杂推理能力涌现的可能性。

---

## 4.3 过程奖励模型（PRM）vs. 结果奖励模型（ORM）

**参考答案：**

**信用分配问题的背景：** 对于数学推理等多步任务，模型可能"猜对答案但推理过程错误"，仅依靠最终答案作为奖励信号，RL 无法有效学习正确的推理模式。

| 对比维度 | ORM（Outcome Reward Model） | PRM（Process Reward Model） |
|----------|----------------------------|----------------------------|
| 奖励时机 | 只在序列结束时给分 | 对思维链**每一步**单独打分 |
| 数据获取难度 | 容易（答案对错即可自动判断） | 困难（需要人工逐步标注） |
| 信用分配质量 | 差（稀疏奖励，难区分好坏步骤） | 好（密集奖励，精准定位错误步骤） |
| 抗奖励作弊能力 | 弱（猜对最终答案即可） | 强（每个推理步骤都必须正确） |
| 计算成本 | 低 | 高（需为每步生成得分） |
| 代表工作 | 大多数 RLHF 系统 | OpenAI PRM800K、Math-Shepherd |

**PRM 的训练数据获取方式：**

1. **人工逐步标注：** 质量最高但成本极高（OpenAI PRM800K 方式）
2. **蒙特卡洛估计：** 从某一步继续采样多次，用最终答案正确率作为该步的"软标签"
3. **过程监督合成：** 用强大的"教师模型"生成带有步骤评分的数据

**PRM 在推理时的应用（Best-of-N 升级版）：** 生成 N 个推理链后，用 PRM 对每条推理链的每一步打分，选择**过程得分最高**的答案，比仅看最终答案的 ORM 更加可靠。

---

## 4.4 DAPO 与 GSPO 简介（前沿进展）

**参考答案：**

**DAPO（Direct Advantage Policy Optimization）— 阿里提出：**

核心改进针对 GRPO 在长链推理训练中的两个问题：
1. **熵坍缩（Entropy Collapse）：** 训练后期模型输出趋同，多样性丧失。DAPO 引入 Token-level Policy Gradient Loss，并移除 KL 散度惩罚，改用 Clip-Higher 机制允许高优势 token 获得更大更新
2. **奖励信号稀疏：** 对全对或全错的 group 过滤掉（不参与梯度更新），确保每个 group 内有有效的正负样本对比信号

**GSPO（Group Sampling Policy Optimization）— 腾讯提出：**

核心观察：GRPO 和 PPO 中的 token-level KL 散度约束（per-token KL）可能不合理，因为 token 之间有强相关性，独立约束每个 token 实际上是过度惩罚。

GSPO 改用 **sequence-level KL 散度**约束：在序列整体层面计算新旧策略的分布距离，同时结合 Group-Relative reward 提供优势估计，在数学推理任务上取得了比 GRPO 更稳定的训练动态。
