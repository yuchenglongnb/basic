# TGV 缺陷检测工艺评估 Agent —— 面试问题准备手册

> 覆盖方向：GraphRAG / 知识图谱 / vLLM 推理优化 / Agent 架构 / 小样本学习
> 每个技术点均附八股文原理，供面试中展开回答。

---

## 目录

1. [GraphRAG 与知识图谱构建](#一graphrag-与知识图谱构建)
2. [vLLM 推理优化（核心亮点）](#二vllm-推理优化核心亮点)
3. [Agent 架构与推理链路](#三agent-架构与推理链路)
4. [小样本学习与因果干预](#四小样本学习与因果干预)
5. [进阶扩展技术点](#五进阶扩展技术点)
6. [面试模拟：完整回答示范](#六面试模拟完整回答示范)
7. [必须准备的具体数字](#七必须准备的具体数字)

---

## 一、GraphRAG 与知识图谱构建

### Q1：GraphRAG 和普通 RAG 有什么区别？你的场景为什么要用 GraphRAG？

**直接回答：**

| 维度 | 普通 RAG | GraphRAG |
|------|----------|----------|
| 索引形式 | 文本块向量化 | 实体/关系图 + 社区摘要 |
| 检索方式 | 语义相似度匹配 | 图遍历 + 全局社区报告 |
| 擅长问题 | 局部精确查询 | 多跳推理 / 全局摘要 |
| 缺陷 | 无法跨段落推理 | 构建成本高、依赖 LLM 抽取质量 |

**八股文原理——普通 RAG 的局限性：**
- 普通 RAG 的本质是"局部语义匹配"：将文档切块 → 向量化 → 检索 Top-K 相似片段 → 喂给 LLM 生成答案
- 核心缺陷是**无法处理多跳推理**。例如"刻蚀液浓度低 → 刻蚀速率不均 → 孔道堵塞 → 孔径偏差"这条因果链横跨多个文档片段，普通 RAG 无法将这些碎片逻辑串联

**GraphRAG 的增量价值：**
- 在向量检索的基础上，额外用 LLM 从文本中抽取实体和关系，构建图结构
- 用 **Leiden 社区检测算法**对图做层次划分，为每个社区生成摘要报告（Community Report）
- 查询时可做 Local Search（精确邻居查询）和 Global Search（全局社区报告汇总），分别对应精确查询和摘要式全局推理

**为什么本项目必须用 GraphRAG：**
工艺参数（激光功率密度、刻蚀时长）与缺陷类型之间是**多跳非线性因果关系**，需要跨工序追踪，普通 RAG 天然处理不了此类问题。

---

### Q2：你的知识图谱里三元组和四元组分别是什么，为什么要区分？

**八股文原理——知识表示：**

- **三元组（静态知识）**：`(主体, 关系, 客体)`，例如 `(刻蚀液浓度低, 导致, 通孔堵塞)`，用于表达不随时间变化的工艺因果规则
- **四元组（动态事件知识）**：`(主体, 关系, 客体, 时间戳)`，用于表达有时序特征的工艺事件，例如某次生产批次在某时间点发生了什么缺陷
- 时间戳可转换为三元组形式 `(主体, 关系_at_T, 客体)` 引入，**在不破坏原有图结构的前提下增加了时间语义关联**

**设计意义：**
静态三元组描述"规律"（哪类原因导致哪类缺陷），动态四元组描述"事件"（某批次何时出现了什么问题），两者结合才能支撑"缺陷溯源"这类需要时序上下文的查询。

---

### Q3：你自定义了哪些实体类型，为什么这么设计？

**实体类型清单（需要能脱口而出）：**

| 实体类型 | 示例 |
|----------|------|
| 设备 | 紫外皮秒激光系统、缺陷检测相机 |
| 化学试剂 | HF/HCl 混合溶液 |
| 加工特征 | 孔径、孔位坐标 |
| 加工方法 | 激光诱导、湿法刻蚀 |
| 缺陷名称 | 孔径过大、孔位偏移 |
| 缺陷原因 | 刻蚀液浓度过低、激光诱导偏差 |
| 缺陷措施 | 刻蚀液浓度提高、激光强度降低 |
| 环境因素 | 环境温度、环境湿度 |

**设计逻辑：**
覆盖"产品-设备-工艺-检测"四个维度，使图谱能完整表达从工艺参数输入到缺陷输出的完整因果链，支撑后续子图挖掘时的多跳路径查找。

---

### Q4：多源知识图谱融合怎么做的？实体对齐是什么原理？

**背景：**
工艺文档、设备日志、缺陷图像三类来源各自构建了子图，同一概念可能表述不同（"HF溶液"和"氢氟酸"），需要对齐后合并。

**八股文原理——实体对齐：**

1. **关系图卷积网络（RGCN）生成嵌入**：对每个子图中的实体，通过 RGCN 聚合邻居信息生成嵌入向量，使语义相近的实体在向量空间中距离更近
2. **邻居采样**：不是用全部邻居聚合，而是采样语义特征更强的邻居参与聚合，过滤噪声节点，降低计算开销
3. **跨子图匹配**：对两个子图中的候选实体对计算嵌入相似度，相似度超过阈值则建立映射关系，合并为同一实体
4. **融合后收益**：聚合后的图结构能捕捉更多与目标实体相关的跨子图上下文信息，同时减少冗余节点

---

### Q5：max_gleanings 设置为 1 是为什么，设大了会怎样？

**八股文原理——Gleanings 机制：**
- Gleanings 是 GraphRAG 保证信息抽取完整性的独特机制，类似**自我反思与再提取**
- 模型看完一段文字抽完实体后，GraphRAG 会追问"你是否遗漏了什么？"然后让模型再抽取一次
- `max_gleanings=N` 意味着最多追问 N 次

**取值权衡：**
- 设为 2/3：图谱更密集，信息更全，但 **LLM 调用次数成倍增加**，在工业离线场景中成本和耗时不可接受
- 设为 1：在信息完整性和计算成本之间取得平衡，适合大规模批量抽取

---

## 二、vLLM 推理优化（核心亮点）

> ⚠️ 这是简历里最容易被深挖的点，以下内容必须做到"条件反射级"熟练。

### Q1：为什么 Ollama 会出现"提取时间缓慢上涨、10 分钟后自动终止"？

**根因分析（要能有条理地讲）：**

1. **Ollama 的定位局限**：Ollama 面向单用户对话场景设计，缺乏高效的并发批处理机制，大量请求涌入时只能串行排队
2. **KV Cache 显存碎片化**：静态连续分配 KV Cache，高并发下显存迅速碎片化，触发 OOM 或显存-内存 Swap，导致延迟断崖式劣化
3. **静态 Batching 的算力浪费**：一批请求必须等最长的那条生成完才能出队，短请求占着卡不走
4. **HTTP 超时机制**：GraphRAG 客户端有默认超时时间（约 10 分钟），Ollama 拥堵导致响应时间超阈值，连接被强制中断

---

### Q2：PagedAttention 原理是什么？（必考）

**八股文原理：**

**传统 KV Cache 分配的痛点：**
- 推理开始时不知道最终生成长度，只能按**最大长度预分配连续显存**
- 实际生成长度 << 最大长度 → **内部碎片**（预留未使用）
- 多个请求的连续块之间 → **外部碎片**（空隙无法利用）
- 传统方式显存浪费率高达 **60%~80%**

**PagedAttention 的解法——借鉴 OS 虚拟内存分页：**

```
逻辑视图（连续）         物理显存（离散）
┌──────────────┐        ┌─────┐ ┌─────┐
│ Block 0      │ ──────>│ 物理块 A │   │ 物理块 C │
│ Block 1      │ ──────>│ 物理块 F │   └─────┘
│ Block 2      │ ──────>│ 物理块 B │
└──────────────┘        └─────┘
      Block Table 维护逻辑→物理映射
```

- 将 KV Cache 划分为固定大小的 **Block**（例如每块存 16 个 Token 的 KV 值）
- 请求按需**动态申请 Block**，不同请求的 Block 在物理显存上可以不连续
- 通过 **Block Table** 维护逻辑块到物理块的映射关系
- 请求结束后，释放其占用的所有 Block，立即可被其他请求复用

**收益：** 显存碎片率降至 **< 4%**，同等显存可支撑原来 **3~4 倍** 的并发请求数

---

### Q3：Continuous Batching 原理是什么？（必考）

**八股文原理：**

**传统静态 Batching 的问题：**
```
时间轴 →
请求A: ████████████████████ (长，20步)
请求B: ████ (短，4步，生成完了也得等A)
请求C:                      ████ (等A结束才能进来)
GPU利用率: 低
```

**Continuous Batching（迭代级调度）：**
```
时间轴 →
请求A: ████████████████████
请求B: ████|请求C立刻顶上
请求C:     ████████████
请求D:                 ████
GPU利用率: 高，始终满负荷
```

- 每个 Token 生成迭代结束后，调度器检查是否有请求已生成 `<EOS>`
- 有则**立刻踢出**，释放 KV Cache Block，并从等待队列**插入新请求**
- GPU 始终保持满负荷运转，整体吞吐量显著提升

---

### Q4：迁移到 vLLM 代码层面改了什么？

**迁移成本极低（这是亮点）：**

```bash
# 启动 vLLM server（兼容 OpenAI API 格式）
python -m vllm.entrypoints.openai.api_server \
  --model Qwen/Qwen3-32B \
  --port 8000
```

```yaml
# settings.yaml 只需修改 api_base，其他代码零改动
completion_models:
  default_completion_model:
    api_base: http://localhost:8000/v1  # 原来是 11434（Ollama）
```

**额外配置：**
- 在客户端侧设置 `concurrent_requests` 控制 GraphRAG 并发上限，防止瞬间 QPS 打爆后端
- 配置 `retry.type: exponential_backoff` 指数退避重试，处理偶发性超时

---

### Q5：除了换推理框架，还有哪些优化思路？（考察架构视野）

**1. 模型路由（Model Routing）**
- 抽取阶段（NER/关系抽取）：使用微调过的 **Qwen2.5-7B/14B**，速度快、显存省
- 查询推理阶段（面向用户）：使用 **Qwen3-32B**，保证推理质量
- 不同任务路由到不同规格的模型，成本和延迟均可优化

**2. 模型量化（Quantization）**
- **AWQ / GPTQ**：训练后量化，将权重从 FP16 降到 INT4，显存占用降约 50%
- **KV Cache 量化**：vLLM 支持将 KV Cache 量化为 FP8，同等显存塞入更多并发
- 量化会带来轻微精度损失，需在推理质量和资源约束之间权衡

**3. 分块策略调优**
- `chunk_size=512, overlap=50` 是当前配置
- chunk 越大：单次 LLM 调用 token 数越多，抽取越完整，但延迟越高
- overlap 过大：冗余计算多；过小：跨块的实体可能被截断
- 需根据文档特点（短工艺文档 vs 长报告）动态调整

---

## 三、Agent 架构与推理链路

### Q1：你的 Agent 完整工作流程是什么？

**端到端流程（要能顺畅地讲）：**

```
用户输入工艺评估查询
        ↓
① 关键实体抽取
   BERT 编码器 → 头实体识别层 + 关系-尾实体联合识别层
   输出：{工艺参数名, 缺陷类型, 设备名, ...}
        ↓
② 子图挖掘
   用实体集在知识图谱上做多跳遍历
   构建包含相关工艺参数、缺陷类型、因果关系的候选子图
        ↓
③ 相关性筛选 + 结构化→自然语言转换
   自定义模板将子图三元组转为自然语言描述
   消除结构化数据与 LLM 理解障碍
        ↓
④ LLM 多轮推理
   结合图谱专业知识 + 模型自身隐含知识
   生成：缺陷溯源分析 + 工艺优化建议 + 可解释性分析
        ↓
⑤ 输出结果
   每条结论可追溯到具体图谱路径
```

---

### Q2：Local Search 和 Global Search 分别在什么场景用？

**八股文原理：**

| | Local Search | Global Search |
|--|--|--|
| 适用场景 | 精确查询 | 宏观摘要查询 |
| 示例问题 | "孔径过大的具体原因是什么" | "整体工艺最容易出问题的环节" |
| 实现机制 | 从查询实体出发，遍历直接相关的邻居节点和关系 | Map：对每个社区 Report 局部总结；Reduce：全局汇总 |
| 答案特点 | 聚焦、具体、细节丰富 | 覆盖面广、高度概括 |
| 底层依赖 | 图谱实体和关系 + 向量检索 | Community Reports（Leiden 社区摘要） |

**本项目的策略：** 根据查询意图动态选择或混合使用，简单精确查询用 Local，全局评估查询用 Global。

---

### Q3：BERT 联合解码实体抽取是什么原理？

**八股文原理——联合抽取 vs 流水线抽取：**

- **流水线方法（Pipeline）**：先做 NER 识别实体，再做关系分类。缺点是错误会在两步之间传播（错误传播问题）
- **联合解码方法（Joint Decoding）**：在一个模型里同时识别实体和关系，共享底层表示，避免错误传播

**本项目的具体结构：**
```
输入文本 → BERT 编码器（捕捉上下文语义，精细表征字/词）
                ↓
        ┌───────────────────┐
        │ 头实体识别层       │ → 识别出"孔径过大"、"刻蚀液浓度"等实体
        │ 关系-尾实体联合层 │ → 识别出"导致"关系和"通孔堵塞"尾实体
        └───────────────────┘
                ↓
        输出完备的查询实体集
```

---

## 四、小样本学习与因果干预

### Q1：小样本学习具体怎么做的？

**背景问题：**
TGV 缺陷样本收集困难，某些稀有缺陷类型可能只有几十张图像，无法支撑传统有监督训练。

**八股文原理——元学习（Meta-Learning）：**

- **核心思想**："学会学习"——通过在**多个不同领域的源任务**上预训练，让模型学习到跨域的共性特征提取能力（而不是学习具体任务的映射）
- **MAML（Model-Agnostic Meta-Learning）**：学习一组初始化参数，使得在新任务上只需少量梯度步就能快速收敛
- **Prototypical Network**：为每类缺陷计算原型向量（类内样本的均值），新样本通过与原型的距离做分类，支持 K-shot（K 张样本）场景

**迁移到目标域（TGV 缺陷）的流程：**
```
自然图像/工业图像（多源任务预训练）
        ↓ 元学习，学习跨域共性
    元学习初始化参数
        ↓ 少量 TGV 缺陷样本微调
    TGV 缺陷检测模型（快速适应）
```

---

### Q2：因果干预是什么？为什么要消除混杂因子？

**问题背景：**
训练数据中某类缺陷总是和某台特定设备同时出现，模型可能学到"设备A → 孔径偏大"这个**虚假因果关系**，而真正的原因是该设备的刻蚀参数设置偏差。

**八股文原理——因果推断（Causal Inference）：**

- **混杂因子（Confounder）**：同时影响"原因变量"和"结果变量"的第三方变量，导致观测到的相关性并非真正的因果性
- **do-calculus（干预操作）**：Pearl 提出的因果干预框架，`P(Y | do(X=x))` 表示"强制将 X 设为 x 时 Y 的分布"，切断 X 与其混杂因子之间的关联
- **实现方式**：在训练过程中通过数据增强、样本重加权或结构因果模型（SCM）消除虚假关联，让模型学到真正的因果关系

**本项目的意义：** 确保知识图谱中的"缺陷原因→缺陷现象"边代表真正的因果关系，而非数据偏差导致的虚假关联，使工艺优化建议更可靠。

---

## 五、进阶扩展技术点

> 主动抛出以下概念，能显著提升面试官对你技术深度的评价。

### FlashAttention

**问题背景：**
标准 Attention 计算需要频繁在 HBM（高带宽显存，容量大但慢）和 SRAM（片上内存，容量小但极快）之间搬运中间结果，形成 IO 瓶颈。

**八股文原理：**

```
标准 Attention：
Q, K, V → 计算 S=QKᵀ → 写回 HBM → 读出做 Softmax → 写回 HBM → 读出乘 V
（大量 HBM 读写，IO 密集）

FlashAttention：
采用 Tiling（分块计算）+ Recomputation（重计算）
不把中间 Attention Matrix 写回 HBM，在 SRAM 中分块算完直接输出
反向传播时重新算一遍中间态（用计算换 IO）
```

**收益：** 整体速度提升 2~4 倍，显存占用降低（不存中间矩阵），且结果与标准 Attention **数学等价**（精确算法）

---

### Speculative Decoding（投机采样）

**问题背景：**
Decode 阶段每次只生成一个 Token，LLM 海量的并行计算单元基本处于闲置状态（GPU 利用率极低）。

**八股文原理：**

```
Draft Model（小模型，快）：连续快速生成 K 个候选 Token
        ↓
Target Model（大模型，准）：对这 K 个 Token 并行验证
        ↓
全部通过 → 一次性接受 K 个 Token（相当于大模型一步生成了 K 个）
某位置拒绝 → 在出错位置纠正，保证输出概率分布不变（无损）
```

**收益：** 在不改变任何输出分布的前提下，延迟降低 **2~3 倍**，尤其适合输出长文本的场景（如工艺优化报告生成）

---

### 量化技术（Quantization）

| 技术 | 原理 | 适用场景 |
|------|------|----------|
| **GPTQ** | 训练后逐层量化，最小化量化误差 | 部署时显存受限 |
| **AWQ** | 保护重要权重通道，激活感知量化 | 精度损失更小，推荐 |
| **KV Cache FP8** | KV Cache 从 FP16 降到 FP8 | 提升并发上限 |
| **INT8 推理** | 权重和激活均量化到 INT8 | 推理速度提升，精度轻微损失 |

**本项目的应用场景：** 对 Qwen3-32B 做 AWQ INT4 量化，显存占用从 ~64GB 降至 ~20GB，使单卡 A100（80GB）可以同时运行模型和支撑更大并发。

---

## 六、面试模拟：完整回答示范

### 场景：面试官问"你引入 vLLM 解决了什么问题，能详细说说吗？"

> "在构建工业级 GraphRAG 时，图谱抽取阶段是一个典型的 Map-Reduce 密集型计算过程，需要并发向大模型发起海量请求处理文本块。
>
> 最初使用 Ollama 部署时，我发现一个规律性问题：**提取时间会随请求量缓慢上涨，大约 10 分钟后触发超时中断**。排查后定位了三个根因：
>
> 第一，Ollama 的 KV Cache 采用**静态连续分配**，高并发下显存迅速碎片化，引发 OOM 或显存-内存 Swap，延迟出现断崖式劣化；
> 第二，**静态 Batching** 机制导致算力严重浪费，短请求生成完了也要等同批次的长请求；
> 第三，GraphRAG 客户端的 HTTP 超时阈值约为 10 分钟，Ollama 拥堵后响应时间超过阈值，连接被强制中断。
>
> 解决方案是将推理后端替换为 **vLLM**。vLLM 的 **PagedAttention** 借鉴操作系统虚拟内存分页机制，将 KV Cache 划分为固定大小的 Block 按需动态分配，通过 Block Table 维护逻辑到物理的映射，彻底消除内外碎片，显存浪费率降到 4% 以下。同时 **Continuous Batching** 实现了迭代级调度，某个请求生成结束后立刻从等待队列插入新请求填满算力空缺，GPU 利用率显著提升。
>
> 由于 vLLM 提供兼容 OpenAI API 的 Server，代码层面只改了 settings.yaml 里的 api_base 端口，迁移成本极低。配合客户端侧的并发信号量控制和指数退避重试策略，最终彻底解决了超时中断问题，知识抽取吞吐量得到了显著提升。"

---

## 七、必须准备的具体数字

> 面试时说不出数字，再好的经历也会大打折扣。以下数字需要实测或计算后填入。

| 指标 | 目标值 | 备注 |
|------|--------|------|
| vLLM vs Ollama 吞吐量提升 | **X 倍** | 需实测，建议测 QPS 或单位时间处理 chunk 数 |
| 知识抽取耗时缩短 | **Y%** | 对比同等文档量下的总耗时 |
| 知识图谱规模 | **实体数、关系数** | 从 GraphRAG 输出统计 |
| 覆盖缺陷类别数 | **≥ 10 类** | 项目指标要求 |
| 知识库数据量 | **≥ 5 万条** | 项目指标要求 |
| 缺陷检测准确率 | **≥ 99.9%** | 需说明计算依据（YOLOv5 基准 + 多任务学习提升） |
| chunk size / overlap 选取依据 | **512 / 50** | 能解释为何这个参数是最优的 |

---

*最后更新：根据项目文档 + settings.yaml 配置 + 工程实践问题整理*