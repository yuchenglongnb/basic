# Part 5 - RAG 八股

> 本章涵盖检索增强生成（RAG）的工作原理、完整流水线、检索优化技术及评估方法。

---

## 5.1 RAG 工作原理 vs. 微调

**参考答案：**

**RAG（Retrieval-Augmented Generation）工作原理：**

"先检索，后生成"模式，将信息检索与文本生成相结合：

1. **检索（Retrieve）：** 将用户问题作为查询，在外部知识库（向量数据库）中搜索最相关的几段信息
2. **增强（Augment）：** 将检索到的相关信息与用户原始问题拼接，形成内容更丰富的**增强提示（Augmented Prompt）**
3. **生成（Generate）：** 将增强后的提示发给LLM，基于上下文生成准确、有事实依据的回答

**RAG主要解决的LLM问题：**

- **知识静态性与过时性：** LLM知识被"冻结"在训练截止日期，RAG连接可随时更新的外部知识库
- **幻觉（Hallucination）：** 通过提供具体上下文，将LLM的回答"锚定"在事实依据上
- **缺乏专业领域/私有知识：** 轻松将模型与私有数据集（公司内部文档等）连接，成为领域专家

**RAG vs. 微调（Fine-tuning）的优势：**

| 维度 | RAG | 微调 |
|------|-----|------|
| 知识更新 | 只需更新数据库文档，无需重新训练 | 需要重新进行训练 |
| 可追溯性 | 清晰展示答案基于哪些源文档，可事实核查 | "黑盒"，无法知道具体来源 |
| 降低幻觉 | 提供事实依据，回答有据可循 | 仍可能在不确定时产生幻觉 |
| 高效费比 | 开发和维护成本远低于微调 | 成本高昂 |
| 个性化 | 可为每个用户/请求动态接入不同知识源 | 难以实现动态个性化 |

---

## 5.2 完整RAG流水线

**参考答案：**

**阶段一：数据准备 / 索引流水线（离线，一次性或周期性执行）**

1. **数据加载（Load）：** 从各种数据源加载原始文档（PDF、Word、网页、Notion、数据库等）
2. **文本切块（Chunk）：** 将长文档切割成更小的、语义完整的文本块
3. **嵌入（Embed）：** 使用嵌入模型（如BGE、M3E等）将每个文本块转换为高维向量
4. **存储（Store）：** 将文本块内容及其向量存储到向量数据库（FAISS、ChromaDB、Pinecone等），建立检索索引

**阶段二：查询 / 推理流水线（在线，用户提问时实时执行）**

1. **用户提问（User Query）：** 接收用户输入的自然语言问题
2. **查询嵌入（Embed Query）：** 使用**与建索引时相同**的嵌入模型，将问题转换为查询向量
3. **向量检索（Retrieve）：** 在向量数据库中进行相似度计算，找出与查询最相似的Top-K个文本块
4. **重排序（Re-rank，可选）：** 引入重排序模型（Cross-Encoder）对Top-K结果精细化打分，选出最优的Top-N个
5. **增强与生成（Augment & Generate）：** 将Top-N文本块与用户问题组合成增强提示，发给LLM生成最终回答

---

## 5.3 文本切块策略：如何选择切块大小和重叠长度

**参考答案：**

**如何选择切块大小（Chunk Size）？**

1. **依据嵌入模型的能力：** 切块大小应小于嵌入模型的最大输入Token数，且大多数嵌入模型在256-512 Token左右效果最好
2. **依据数据类型和结构：**
   - 结构化文档（论文、报告）→ 按段落/标题语义切块，保留语义完整性
   - 非结构化长文本 → 固定长度切块
   - 代码 → 按函数或类来切块
3. **依据预期查询类型：**
   - 具体问题（精确定位某一句话）→ 较小切块
   - 宽泛问题（综合多段落信息）→ 较大切块

**如何选择重叠长度（Overlap）？**

- 作用：防止语义信息在切块边界被硬生生地切断
- 经验法则：设置为切块大小的**10%-20%**（例如，chunk_size=1024时，overlap设置128-256）

**背后的权衡：**

| 维度 | 大块 | 小块 |
|------|------|------|
| 上下文 | ✅ 上下文丰富，适合宽泛问题 | ❌ 上下文不足，可能需要拼接多个块 |
| 检索精度 | ❌ 信噪比低，精确匹配较差 | ✅ 信息密度高，精确匹配好 |
| 计算成本 | ❌ 更长的LLM上下文，成本更高 | ✅ 计算成本低 |
| 语义完整性 | ✅ 完整 | ❌ 容易切断连续上下文 |

---

## 5.4 如何选择嵌入模型及评估指标

**参考答案：**

**如何选择嵌入模型？**

1. **参考公开排行榜：** 优先查看 **MTEB（Massive Text Embedding Benchmark）**，选择在**检索（Retrieval）**任务上得分高的模型；中文场景参考 C-MTEB
2. **考虑具体应用场景：** 专业领域（医疗、法律）可选在该领域预训练/微调过的模型；多语言支持；模型大小与推理速度的平衡
3. **私有 vs. 开源：**
   - **私有（如OpenAI Ada）：** 性能强大，使用方便，但存在数据隐私风险且成本较高
   - **开源（如BGE、M3E、Jina-embeddings）：** 可本地部署，数据安全可控，成本低

**评估指标：**

- **nDCG@k（Normalized Discounted Cumulative Gain）：** 综合衡量检索结果的**相关性**和**排名**，是检索任务最核心的指标
- **Recall@k：** 在前k个结果中召回了多少比例的相关文档
- **MRR（Mean Reciprocal Rank）：** 第一个相关文档出现在第几位
- **语义文本相似度（STS）：** Spearman/Pearson相关系数，衡量模型计算的余弦相似度与人类判断的相关性
- **Hit Rate：** 是否至少包含一个相关文档（基础"及格线"指标）

---

## 5.5 提升RAG检索质量的技术

**参考答案：**

**一、增强检索器**

**1. 混合搜索（Hybrid Search）**

- 将**稀疏检索（BM25，基于关键词）**与**密集检索（向量搜索，基于语义）**结合
- 兼顾精确术语匹配和语义模糊匹配，效果通常远超单一检索方法

**2. 重排序（Re-ranking）**

- 两阶段流程：先用向量搜索快速召回Top-50候选集，再用**Cross-Encoder**精细化重排，选出Top-5
- Cross-Encoder可直接比较查询和文档的文本，捕捉更细粒度的相关性

**二、优化查询**

**3. 多查询检索（Multi-Query Retrieval）**

- 让LLM从不同角度生成多个不同的查询，合并所有查询的检索结果

**4. HyDE（Hypothetical Document Embeddings）**

- 让LLM先针对问题生成一个"假设性"答案，用这个答案的嵌入去检索（答案的文本在形式上与目标文档更相似）

**5. 子问题查询（Sub-Querying）**

- 将复杂问题先分解成多个简单子问题，分别检索后汇总结果

**三、优化索引结构**

**6. 小块引用大块（Small-to-Large Chunking）**

- 索引时切成小块用于精确检索，但每个小块保留指向更大"父块"的引用
- 检索时用小块匹配以获得高精度，最终送给LLM的是包含更丰富上下文的父块

**7. 图索引（Graph Indexing）**

- 用LLM提取文档中的实体和关系，构建知识图谱
- 对需要多跳推理、理解实体关系的查询非常有效

---

## 5.6 "Lost in the Middle"问题

**参考答案：**

**现象描述：** LLM在处理长上下文时，倾向于**更好地利用位于上下文开头和结尾的信息，而忽略或遗忘位于中间部分的信息**。

**在RAG中的影响：**

我们通常将检索到的Top-K文档拼接后送给LLM：`[原始问题] + [文档1] + [文档2] + ... + [文档K]`

- 文档1和文档K会得到充分关注
- 位于中间的文档，即使包含关键信息，也可能被LLM忽略
- 导致精心设计的检索（包括重排序）的效果大打折扣

**缓解方法：**

1. **文档重排序：** 将**最相关**的文档放置在上下文的**开头和结尾**，次要相关的放在中间，确保关键信息处于LLM的"注意力甜点区"

2. **减少检索的文档数量：** 严格控制K值（如Top-3或Top-5），提高送入LLM的文档质量，减少噪声

3. **指令化提示：** 在Prompt中明确指示模型要关注所有提供的上下文，不要忽略任何一份文档

4. **对LLM进行微调：** 构建特定微调数据集，要求模型必须利用中间位置的信息才能正确回答，从根本上训练模型不忽略中间内容（成本最高但最根本）

---

## 5.7 全面评估 RAG 系统性能

**参考答案：**

**第一阶段：检索性能评估**（评估检索器"找得对、找得全"）

需要（问题，相关文档ID）标注数据集：

- **上下文精确率（Context Precision）：** 检索到的文档中有多少是真正相关的（信噪比）
- **上下文召回率（Context Recall）：** 所有相关文档中有多少被成功找回来（查全率）
- **Hit Rate：** 至少包含一个相关文档的基础指标
- **MRR / nDCG@k：** 衡量排名质量

**第二阶段：生成性能评估**（评估LLM"忠实、准确、有用"地生成答案）

- **忠实度/可溯源性（Faithfulness）：** 生成的答案是否完全基于所提供的上下文，是否存在幻觉
- **答案相关性（Answer Relevancy）：** 生成的答案是否直接、清晰地回答了用户的原始问题
- **答案正确性（Answer Correctness）：** 答案中的信息是否事实准确（与黄金标准答案比较）

**自动化评估框架：**

- **RAGAS**、**ARES**、**TruLens** — 使用LLM-as-a-Judge思想，自动化计算上述指标，极大提高评估效率

---

## 5.8 何时使用图数据库/知识图谱增强RAG

**参考答案：**

向量数据库擅长**语义相似度**的模糊匹配，知识图谱擅长**实体与关系**的精确查询。

**选择使用图数据库/知识图谱的核心场景：**

**1. 需要多跳推理（Multi-hop Reasoning）的复杂问题**

- 无法通过单个文档回答，需要沿着关系链进行多次"跳转"
- 举例："Llama 2的作者所在的公司的CEO是谁？"（三跳：Llama 2 → 作者 → Meta → CEO）

**2. 数据本身具有强结构和关联性**

- 包含大量实体和明确关系（雇佣、投资、拥有、位于）
- 举例：金融领域的公司股权结构、欺诈检测的资金流动网络、医疗领域的药物-基因-疾病关系

**3. 需要提供高度可解释性的答案**

- 在严肃应用（金融风控、医疗诊断）中，不仅需要答案，还需要清晰的推理路径
- 知识图谱的查询路径本身就是一种直观、可解释的证据链

**实践中的常见模式（增强而非替代）：**

1. 先查询知识图谱，找到核心实体和事实（结构化推理）
2. 以图谱中的实体为线索，去向量数据库中检索相关的非结构化文本（详细背景）
3. 将两方面信息汇总给LLM生成综合答案

---

## 5.9 复杂 RAG 范式：多次检索与自适应检索

**参考答案：**

**1. 迭代式检索（Iterative Retrieval）— 例如 Self-RAG, Corrective-RAG**

将RAG从单向流水线变成**循环、自我修正**的过程：

1. 首次检索并生成初步答案
2. LLM对初步答案和上下文进行"反思"，评估信息是否足够
3. 如果不足，主动生成新的、更具针对性的查询，进行第二轮检索
4. 整合新旧信息，生成更完善的最终答案

**2. 自适应检索（Adaptive Retrieval）— 例如 FLARE**

不在生成前一次性检索，而是在**生成过程中"按需"检索**：

1. LLM开始直接生成答案
2. 当预测到即将生成不确定的事实性信息时（概率分布很平坦），**暂停**生成
3. 插入特殊标记，主动提出需要查询的问题
4. 检索到信息后继续向下生成
- **优势：** 只在需要时才检索，避免预先检索大量无关信息

**3. 多源数据RAG（Multi-Source RAG）**

Agent从**多种不同类型数据源**中智能检索和整合：向量数据库（非结构化文本）+ 知识图谱（实体关系）+ SQL数据库（精确统计数据）+ 搜索引擎（实时信息）。本质上是**Agent驱动的RAG**。

---

## 5.10 RAG 系统在实际部署中的挑战

**参考答案：**

1. **数据处理与维护的复杂性：**
   - 异构数据源（PDF、HTML、JSON）需要不同的分块策略，难以设计通用方案
   - 知识库实时更新：如何高效地在源文档修改时同步更新向量索引

2. **性能瓶颈：延迟与成本：**
   - "检索+生成"天然比直接调用LLM慢，实时交互场景下需要极致优化
   - 大规模文档嵌入、向量数据库运行、LLM API调用都是持续成本

3. **端到端的评估与监控：**
   - 生产环境中难以有带标准答案的数据集
   - 需要建立完善的监控系统，发现是检索模块性能下降还是生成模块产生更多幻觉

4. **处理"无答案"和"上下文外"问题：**
   - 知识库中不包含答案时，系统容易基于不相关检索结果强行生成误导性答案
   - 需要模型具备判断"检索到的内容与问题无关"的能力，并给出明确的"无法回答"响应

5. **安全与隐私：**
   - 企业环境需要集成文档访问控制，确保用户只能检索有权查看的内容
   - 防范提示注入攻击（恶意用户在查询中嵌入恶意指令，或被索引文档本身包含恶意内容）

---

## 5.11 搜索系统 vs. RAG 的区别

**参考答案：**

| 特征 | 搜索系统（如Google Search） | RAG系统 |
|------|--------------------------|---------|
| **核心任务** | 信息检索（Find Documents） | 问题回答（Give Answers） |
| **输出** | **文档列表**（相关源文档的排序列表） | **自然语言答案**（综合、提炼后的成品） |
| **用户角色** | 用户是**主动**的，需要自己阅读和总结 | 用户是**被动**的，直接获得成品答案 |
| **核心组件** | 索引器 + 排序器 | 索引器 + 排序器 + **生成器（LLM）** |

**一个简单的比喻：**

- **搜索系统**是图书馆管理员：告诉你哪几本书可能有答案，你自己去翻阅
- **RAG系统**是领域专家：查阅相关书籍后，直接告诉你答案，并注明参考来源

---

## 5.12 开源RAG框架及选型（开放题）

> **提示：** 结合自己使用过的框架（LangChain、LlamaIndex、RAGFlow、Haystack、DSPy等）回答：
>
> **选型策略：**
> - **高度定制化需求、以Agent为核心** → LangChain / LlamaIndex（代码库形态，"为了灵活性和控制力"）
> - **快速原型验证、追求最佳实践开箱即用** → RAGFlow / Haystack（平台形态，"为了效率和易用性"）
>
> **实践经验：**
> - 项目初期用平台框架搭建基线，验证业务价值后，如需更深度优化再用LangChain/LlamaIndex重构
> - 说明具体选型理由、遇到的问题、最终评价指标（检索准确率、答案忠实度、响应延迟、成本等）
