# Part 6 - 模型评估与 Agent 评估

> 本章涵盖LLM与Agent的评估方法、常用基准测试、评估范式设计及部署后的持续监控策略。

---

## 6.1 传统NLP指标（BLEU/ROUGE）的局限性

**参考答案：**

BLEU和ROUGE的核心思想是比较生成文本与"参考答案"在**表层词汇（n-gram）上的重合度**，这对于评估现代LLM的生成质量存在巨大局限性：

**1. 语义理解的缺失**

- 只关心词汇表面匹配，不理解语义
- "今天天气很好"和"今天日光很灿烂"含义相近，但BLEU得分很低；反之，词汇高度重合但逻辑混乱的句子可能得高分

**2. 无法评估事实准确性**

- 无法检测LLM的幻觉问题，一个流畅但包含完全错误事实的回答可能得高分

**3. 忽略了多样性与创造性**

- 对于开放式生成任务，不存在唯一"标准答案"，任何与参考答案不同但同样优秀的回答都会被"惩罚"

**4. 对长文本的评估能力差**

- 无法评估长篇文本的**连贯性、逻辑性和结构性**，只能进行局部、零碎的词汇匹配

**5. 对推理过程的无视**

- 无法评估模型的"思维链"是否正确，只能比较最终答案的字符串

---

## 6.2 主流LLM综合性基准测试

**参考答案：**

**1. MMLU（Massive Multitask Language Understanding）**

- **侧重点：** 知识的广度与学科问题解决能力
- **内容：** 57个不同科目（从初等数学到专业级法律、医学），全部为四选一单项选择题
- **评估目的：** 检验模型是否具备渊博的、跨学科的知识储备

**2. Big-Bench（Beyond the Imitation Game Benchmark）**

- **侧重点：** 探索LLM的能力边界和未来潜力
- **内容：** 社区协作创建，超过200个任务，测试当前LLM难以解决的能力（常识推理、逻辑、物理直觉等）
- **评估目的：** 预测未来——找到规模跨越临界点后可能"涌现"的新能力，衡量通用智能水平

**3. HumanEval（Human-Labeled Evaluation）**

- **侧重点：** 代码生成与编程能力
- **内容：** 164个编程问题，每题提供函数签名、文档字符串（docstring）和单元测试
- **评估方法：** **pass@k** 指标——模型生成k个代码样本，至少有一个能通过所有单元测试则算通过
- **评估目的：** 衡量编写正确、可用代码的能力

**其他重要基准：**

- **GSM8K：** 小学水平数学应用题，测试多步思维链推理能力
- **ARC（AI2 Reasoning Challenge）：** 需要科学常识和推理的有挑战性选择题
- **HellaSwag：** 常识推理，选择最合理的句子来续写给定情景

---

## 6.3 LLM-as-a-Judge

**参考答案：**

**定义：** 利用一个功能强大的前沿LLM（称为"裁判模型"）来评估另一个被测试LLM的输出质量，通过设计明确的评估准则（rubric）来自动化地为模型输出打分。

**优点：**

1. **可扩展性与效率：** 近乎实时地、大规模地评估海量模型输出，极大地加速模型迭代反馈循环
2. **一致性：** 裁判模型和评估提示固定，标准完全一致，避免人类标注者的主观差异
3. **可定制化：** 通过设计不同的评估准则，轻松地从任意维度（简洁性、创造性、安全性）评估输出

**潜在的偏见：**

1. **位置偏见（Position Bias）：** 倾向于偏爱第一个呈现给它的答案
2. **冗长偏见（Verbosity Bias）：** 倾向于给更长、更详细的回答打更高的分数，即使内容有冗余
3. **自我偏好/风格偏见（Self-Preference Bias）：** 更偏爱与自己生成风格相似的回答，惩罚不同风格但同样优秀的模型
4. **有限的知识与推理能力：** 裁判模型本身可能犯事实性错误，无法识别被测模型回答中非常细微的专业领域错误
5. **过于"宽容"：** 对有害或不当内容的判断可能比人类更宽容

---

## 6.4 设计特定能力的评估方案

**参考答案：**

**评估方案设计流程：** 定义能力 → 构建数据集 → 确定评估方法

**1. 衡量"事实性/幻觉水平"**

- **数据集构建：**
  - 构建答案可从确定知识源（Wikipedia、公司文档、数据库）中找到的QA问题集
  - 设计诱导模型产生幻觉的对抗性问题（询问关于不存在的人物或事件）
- **评估方法：**
  - 精确匹配/关键词匹配（简单事实性问题）
  - LLM-as-a-Judge判断答案是否与源知识相符或矛盾
  - 自动化框架（RAGAS的Faithfulness指标）

**2. 衡量"推理能力"**

- **数据集构建：** 使用GSM8K、LogiQA、Big-Bench Hard等专业推理基准；设计需要特定推理路径的任务
- **评估方法：**
  - **结果评估（Outcome-based）：** 判断最终答案是否正确
  - **过程评估（Process-based）：** 评估思维链推理步骤是否合乎逻辑（能更深入了解模型的推理过程）

**3. 衡量"安全性"**

- **数据集构建：**
  - 公开对抗性数据集（AdvBench、SafetyBench）
  - 红队测试（Red Teaming）：人类专家创造性地构建新的攻击性提示
- **评估方法：**
  - 将模型回答输入安全分类器（另一个LLM或专用分类模型）
  - 核心指标：**拒绝率（Refusal Rate）**（成功拒绝有害问题的比例）和**误伤率（False Refusal Rate）**（错误拒绝正常问题的比例）

---

## 6.5 评估 Agent vs. 评估基础 LLM 的区别

**参考答案：**

评估对象从**静态的、单轮的"文本生成器"**，转变为**动态的、多轮的、与环境交互的"决策者"**，这引入了根本性的复杂性。

**困难和复杂性的根源：**

1. **交互性与状态空间：** Agent是有状态的（stateful），与环境多步交互，可能的行为轨迹数量是天文数字
2. **环境的动态性与不确定性：** 真实环境（网页、API）是动态变化的，评估结果难以复现
3. **非确定性：** 同一个Agent在相同初始任务下，两次执行结果可能完全不同
4. **任务的开放性：** 没有唯一的正确答案，无法定义简单的"正确/错误"指标

**评估维度的不同：**

| 评估维度 | 基础LLM | Agent |
|---------|---------|-------|
| 核心评估对象 | **单个回答的质量** | **整个任务完成过程** |
| 主要维度 | 准确性、流畅性、相关性、安全性 | 任务成功率、效率、鲁棒性、自主性 |
| 新增的过程维度 | 无 | 成本（LLM调用次数/API费用）、延迟（总完成时间）、步骤数、错误处理能力、一致性 |
| 评估方法 | 静态数据集上的基准测试（MMLU, HumanEval） | **交互式环境**中的基准测试（WebArena, AgentBench） |

---

## 6.6 专门评估 Agent 能力的基准测试

**参考答案：**

**1. WebArena**

- **专注领域：** 网页浏览与操作
- **环境：** 高度逼真、独立的网页环境模拟器，复刻了电商、论坛、软件开发协作工具等真实网站
- **任务举例：** 在电商网站上找到满足特定要求的商品并加入购物车；在论坛上预订会议室
- **评估方式：** 基于最终网页状态的程序化判断

**2. AgentBench**

- **专注领域：** 通用Agent能力的综合评估
- **内容：** 8个不同环境（Linux终端、SQL数据库、知识图谱、文字冒险游戏等）
- **评估目的：** 全面评估Agent在不同场景下的能力

**3. GAIA（General AI Assistants）**

- **专注领域：** 模拟人类使用真实工具完成复杂任务
- **特点：** 极具挑战性，问题需要组合使用多种工具（网页浏览器、代码解释器、文件操作）才能解决
- **任务举例：** "找出引用了论文A和论文B的所有论文中，被引用次数最高的那篇的第三位作者是谁？"

**基准测试如何构建测试环境和任务：**

- **沙箱化与可复现性：** 使用Docker容器封装独立环境；本地托管网站副本；API调用重定向到模拟服务器
- **目标导向的任务设计：** 以高层次目标形式给出，而不是具体步骤指令，覆盖信息检索、工具使用、推理规划等多种能力
- **程序化验证：** 自动检查最终环境状态（文件内容、购物车状态、答案字符串）是否满足成功条件

---

## 6.7 Agent 任务完成的过程指标

**参考答案：**

除了最终结果的正确性（Task Success），还需关注以下过程指标：

**1. 效率（Efficiency）**

- **成本（Cost）：**
  - Token消耗量（所有步骤消耗的总Token数）
  - API调用费用（完成一次任务的总花费）
- **延迟（Latency）：**
  - 总耗时（Wall-clock Time）：从任务开始到结束所经过的真实时间
- **步骤数（Number of Steps）：** Agent执行"思考-行动"循环的总次数，步骤越少说明规划能力越强

**2. 鲁棒性（Robustness）**

- **错误处理能力：** 当工具返回错误、网页加载失败或遇到预期外状态时，Agent能否识别问题并采取纠正措施（尝试不同工具、修正参数、重新规划）
- **抗干扰能力：** 在环境中加入噪声或误导性信息，评估Agent成功率下降了多少

**3. 自主性与对齐（Autonomy & Alignment）**

- **需要人类干预的次数：** 更自主的Agent需要人类帮助的次数更少
- **行为可解释性：** Agent的"思考"过程是否清晰、合乎逻辑，是否能让人类理解决策依据
- **计划遵循度：** Agent实际执行在多大程度上遵循了自己预先生成的计划

---

## 6.8 红队测试（Red Teaming）

**参考答案：**

**定义：** 组织一个专门的团队，像"攻击者"一样主动地、创造性地寻找和利用LLM或Agent的漏洞、缺陷和非预期行为，以评估和提升其安全性和鲁棒性。

**在发现安全漏洞方面的角色：**

- **绕过安全护栏：** 设计"越狱提示"，诱导模型生成有害内容（暴力、色情、仇恨言论、违法活动指导）
- **提示注入（Prompt Injection）攻击（针对Agent）：**
  - 模拟恶意用户或被污染的外部数据劫持Agent控制流
  - 诱导Agent泄露敏感信息、滥用工具（发送垃圾邮件、删除文件）、改变原始目标
- **发现资源滥用漏洞：** 尝试让Agent陷入无限循环或执行高消耗操作

**在发现偏见方面的角色：**

- **暴露刻板印象：** 设计涉及特定人群（种族、性别、国籍、职业）的引导性问题，暴露歧视性回答
- **测试政治与社会偏见：** 询问有争议的话题，评估模型立场是否中立
- **揭示代表性不足问题：** 探索模型在处理非主流文化或群体相关问题时的准确性

---

## 6.9 设计人工评估准则和流程以保证客观性和一致性

**参考答案：**

**一、设计合理的评估准则（Rubric）**

1. **明确且原子化的评估维度：** 将模糊的"好/坏"分解为具体、相互独立的维度（准确性、完整性、简洁性、安全性等）

2. **量化的评分标准：** 使用李克特量表（1-5分）或二元判断（是/否），为**每一个分数等级**提供清晰、明确的定义（如5分=完全准确；4分=基本准确但有细微瑕疵；1分=完全错误）

3. **提供丰富的示例：** 为每个分数等级提供**典型的正面和负面示例（Golden examples和counter-examples）**，极大帮助标注者校准判断标准

**二、设计合理的评估流程**

1. **标注者培训与校准：** 系统培训所有标注者理解评估准则；召开校准会，让大家对同一批样本打分，公开讨论对齐差异

2. **盲评（Blind Evaluation）：** 标注者不知道回答来自哪个模型，消除品牌偏见和先入为主

3. **多次独立评估与一致性检验：** 每个样本至少由2-3名标注者独立评估；使用**Cohen's Kappa或Fleiss' Kappa**衡量标注者间信度（IAA）；IAA过低时需返回修改评估准则

4. **成对比较（Pairwise Comparison）：** 对于A vs. B对比，让人类判断"哪个更好"比分别打绝对分数更容易且可靠，有效减少个体打分尺度差异

5. **建立仲裁机制：** 对标注者分歧较大的"疑难案例"，由高阶专家或委员会进行最终仲裁

---

## 6.10 持续监控和评估已部署LLM应用/Agent服务

**参考答案：**

应对**模型漂移（Model Drift）**和**数据漂移（Data Drift）**，需要建立"采集 → 监控 → 分析 → 迭代"的闭环体系：

**1. 采集与日志（Collection and Logging）**

- **全面日志：** 记录每次请求的完整交互数据（用户输入、中间步骤、最终输出、工具调用、延迟、Token消耗）
- **用户反馈：** 在产品界面嵌入"顶/踩"按钮、打分、一键报告问题等机制

**2. 自动化监控（Automated Monitoring）**

- **监控代理指标（Proxy Metrics）：**
  - 输入指标：问题长度、主题分布、提问语言
  - 输出指标：回答长度、JSON格式错误率、拒绝回答率
  - 过程指标（Agent）：平均执行步数、工具调用频率、工具调用失败率
- **自动化质量评估：**
  - 定期随机抽样生产流量
  - 使用LLM-as-a-Judge对抽样样本自动打分（有害性、跑题率等）
  - 与内部维护的"黄金评估集"对比，监控关键问题上的表现稳定性

**3. 人工审核与分析（Human Review and Analysis）**

- **定期人工审计：** 定期审核随机样本、用户反馈的坏案例、自动化监控发现的异常案例
- **根本原因分析：** 判断问题来源——是LLM能力退化？是Agent规划逻辑有误？是某个工具API发生变更？

**4. 反馈闭环与模型迭代（Feedback Loop and Model Iteration）**

- **持续的数据管理：** 将生产环境中发现的有价值案例（特别是失败案例）清洗、标注后，持续加入**评估集**和**微调数据集**
- **定期再训练/微调：** 根据积累的新数据，定期对模型进行微调，适应新的数据分布和用户需求
- **A/B测试：** 上线新版本模型或Agent逻辑时，小流量验证新版本性能是否优于旧版本，确保每次迭代都是正向的


---

## 6.11 大模型评测的主要范式与挑战

**参考答案：**

**四大评测范式：**

**1. 基准测试评测（Benchmark Evaluation）**

分为三种子类型：
- **判别式（Multiple-choice）：** MMLU、ARC、HellaSwag 等，自动计算准确率，速度快、可复现
- **生成式（Open-ended Generation）：** HumanEval（代码）、GSM8K（数学）等，评估生成质量
- **对话式（Conversational）：** MT-Bench（多轮对话），使用 LLM-as-a-Judge 打分

**2. 人工评测（Human Evaluation）**

黄金标准，适用于创意写作、细粒度对齐等复杂任务；成本高，难以规模化（详见 6.9）

**3. LLM-as-a-Judge（详见 6.3）**

**4. 竞技场评测（Arena-style Evaluation）**

- 代表：**Chatbot Arena（LMSYS Borg）**
- 原理：用户提问后随机看到两个匿名模型的回答，投票选出更好的；通过 Elo 评分系统动态排名
- **优点：** 数据来自真实用户交互，评估分布最贴近实际使用场景
- **缺点：** 数据收集慢；存在选择偏差（参与竞技场的用户不代表所有用户群体）

**三大核心挑战：**

1. **基准污染（Benchmark Contamination / Data Leakage）：** 模型训练数据可能包含了测试集问题，导致分数虚高但不代表真实能力。解决方向：使用模型发布后才出现的新数据构建评测集；定期轮换评测基准

2. **Goodhart's Law（古德哈特定律）：** 当模型被专门优化来提高某个指标分数时，该指标就失去了代理真实能力的有效性。表现为模型在 MMLU 上得分极高但实际使用体验一般

3. **评测维度不完整：** 现有基准难以全面覆盖"有用、诚实、无害"的所有子维度，特别是长文本连贯性、真实世界常识、跨文化理解等能力仍缺乏标准化评测方案